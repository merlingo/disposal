// JavaScript source code for importing config file 
function readXML(filename) {

}
var config = function getConfig() {
    //import xml code and fill the config attributes
    filename = ".xml";
    var configfile = readXML(filename);
    var kullanilanHizmetler = "";
    var isStrict = "";
    var streamable = "";
    var queriable = "";
    var connectFrom = "";
    var contype="" //how to use this connector, by web request (w), or by function-call (fc)
}

module.exports = config();